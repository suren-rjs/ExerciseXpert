name: exercise_expert
description: Workouts Showcase
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  lottie: ^2.3.2
  flutter_secure_storage: ^8.0.0
  get: ^4.6.5
  http: ^0.13.1
  avatar_glow: 1.2.0
  pagination_flutter: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  mockito: ^5.0.15
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/

